{
  "name": "visor-clima-ciudad",
  "version": "1.0.0",
  "description": "App híbrida para consultar el clima por ciudad usando OpenWeatherMap API.",
  "scripts": {
    "start": "parcel src/index.html --open",
    "build": "parcel build src/index.html"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "parcel": "^2.9.3"
  }
}
